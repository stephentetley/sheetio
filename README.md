# sheetio

A Flix library built on top of Apache POI to read and write Excel 
files.

The original verion of this library predated effects in Flix, it 
has been moved into the folder `_temp` and needs a complete 
rewrite. 

The current code in `src` is just a test to call the POI jars 
from Flix and is not a useful library.

Status from May 2023 - not working.

